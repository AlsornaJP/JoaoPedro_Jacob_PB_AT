spring:
  main:
    banner-mode: off
  application:
    name: srs
  datasource:
    url: jdbc:h2:mem:alumni
    username: jp
    password: 123
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
  h2:
    console:
      enabled: true
      path: /h2-console
